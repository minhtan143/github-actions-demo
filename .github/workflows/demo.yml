name: Demo CI

on:
  repository_dispatch:
    types: [deploy-pages]
    inputs:
      version:
        required: true

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Run Demo
      run: |
        echo "Deploying version: ${{ github.event.inputs.version }}"
